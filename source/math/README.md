# math

`import xtb.math;` provides allocation-free scalar, vector, matrix, transform,
projection, random, and noise utilities. Matrices are column-major and multiply
column vectors.

See [`math_demo.d`](../../examples/math_demo.d).
