# parser

`import xtb.parser;` provides arena-backed parser combinators with sequencing,
transformation, repetition, explicit backtracking/commit control, recursive
rules, tokenization, and expression precedence. JSON and arithmetic grammars are
included.

See the [parser guide](docs/guide.md).
