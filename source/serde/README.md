# serde

`import xtb.serde;` provides serialization and deserialization for supported
BetterC values using compile-time metadata and attributes. Decoded owning values
use explicit ownership.

See [`serde_demo.d`](../../examples/serde_demo.d) for encoding, decoding,
attributes, borrowed values, and owning values.
