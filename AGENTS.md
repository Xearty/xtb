This is a C++ library collection. However, the libraries need to be migrated to the D language. The xtbd directory must contain all D code. Treat it as a totally separate project. The D project shouldn't mirror exactly what the C++ project does. You have freedom to modify and move code, fix errors and use modern tooling for the sake of being idiomatic. All D code must be compatible with betterC. Don't modify the original C++ code. Don't reuse files from the C++ project. The new D project directory must be entirely independent. Don't reuse the toplevel flake or justfile.